export interface MapAnimationConfig {
  zoom: {
    /** zoom to extend including the following expansion factor */
    expandFactor: number;
    /** animation duration in ms */
    duration: number;
  };
}
