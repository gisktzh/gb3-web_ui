/**
 * PanelClasses that can be used as an input to MatDialog; they reference a specific material-overrides override and style a dialog
 * accordingly.
 */
export enum PanelClass {
  API_WRAPPER_DIALOG = 'api-wrapper-dialog'
}
