/**
 * These variables are overwritten by update-version.js when running `npm run update-version`. If the strings are adjusted, always
 * remember to adjust the replacements inside `update-version.js` as well.
 */
export const APPLICATION_VERSION = 'LOCAL_DEV_VERSION';
export const APPLICATION_RELEASE = 'LOCAL_DEV_RELEASE';
